import { Clover, type LucideProps } from "lucide-react";

/** FridgeAI · CloverKY 브랜드 클로버 아이콘 (AI 채팅·도우미 등) */
export function CloverIcon(props: LucideProps) {
  return <Clover aria-hidden {...props} />;
}
